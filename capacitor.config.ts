import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bank-card-wallet-extension-angular',
  webDir: 'www',
};

export default config;
